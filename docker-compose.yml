services:
  comptapilot:
    build: .
    env_file:
      - .env
    ports:
      - "5000:5000"

  postgres:
    image: postgres:16
    env_file:
      - .env
    environment:
      POSTGRES_USER: comptapilot
      POSTGRES_PASSWORD: comptapilot
      POSTGRES_DB: comptapilot
